# Test profile — disable infrastructure that requires Redis/RabbitMQ connections
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration,\
  org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

# Use H2 in-memory DB for tests (no real DB needed)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Minimal JWT secret for tests
app.security.jwt.secret=test-secret-key-for-unit-tests-minimum-256-bits-long-enough
app.security.jwt.expiration-ms=3600000
app.security.jwt.refresh-expiration-ms=86400000
